import React from "react";
import { SafeAreaView, TextInput, TouchableOpacity, StyleSheet, Button, View } from "react-native";

const Home = () => {    

    return(
        <SafeAreaView>
            
        </SafeAreaView>
    )
}

const styles = StyleSheet.create({
    
    
})

export default Home;